<%= form_for(@newaguy,:remote=>true) do |f| %>
  <% if @newaguy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newaguy.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% @newaguy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
<%= f.label :name %><br />
<%= f.text_field :name %>
</div>
<div class="field">
<%= f.label :whatyousay? %><br />
<%= f.text_field :bio %>
</div>
<div class="field">
<%= f.label :date %><br />
<%= f.date_select :birthday %>
</div>
<div class="actions">
<%= f.submit %>
</div>
<% end %>
